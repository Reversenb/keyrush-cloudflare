// =========================================================================
// ✅ ตรวจคำตอบคำสั่ง — ไม่สนตัวพิมพ์เล็ก/ใหญ่ทั้ง Linux และ Windows
//
// เหตุผล: bash หาคำสั่งด้วยการเปิดไฟล์ใน PATH ตัวตัดสินจึงเป็น "ระบบไฟล์"
// ไม่ใช่ตัวเชลล์ — บนเครื่องที่ผู้เรียนใช้จริงส่วนใหญ่ (Git Bash / WSL บน /mnt/c,
// macOS APFS) ระบบไฟล์ไม่แยกตัวพิมพ์ พิมพ์ MKDIR ก็สร้างโฟลเดอร์ได้จริง
// จะมีก็แต่เซิร์ฟเวอร์ Linux แท้ (ext4/xfs) ที่แยกตัวพิมพ์แล้วขึ้น command not found
//
// เกมจึงเลือกยึดตามเครื่องที่ผู้เล่นใช้จริง = รับทั้งตัวเล็กและตัวใหญ่
//
// ⚠️ ฝั่งหน้าเว็บที่ตัดสินถูก/ผิดเองต้องใช้กติกาเดียวกันเป๊ะ
//    (ดู Keyrush-frontend/lib/answerCheck.ts) ไม่งั้นจอบอกผิดแต่ server บอกถูก
// =========================================================================

export const isAnswerCorrect = (_os: string, userAnswer: string, expected: string): boolean =>
  userAnswer.trim().toLowerCase() === expected.trim().toLowerCase()
